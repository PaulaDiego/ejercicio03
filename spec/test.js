var modulo = require('../index.js');

describe("A suite is just a function", function() {
  var a;

  it("and so is a spec", function() {
    resultado = modulo.sumar(4,5);

    expect(resultado).toBe(9);
  });
});
    


